package days

import Solution
import kotlin.system.exitProcess

class DayNull : Solution {
	override val day: Int = 1 // To avoid Assertion Errors

	override fun solve1(): String {
		exitProcess(0)
	}

	override fun solve2(): String {
		exitProcess(0)
	}
}